import { useState, type FormEvent, type ChangeEvent } from 'react';
import { X } from 'lucide-react';
import { useAuth } from '../../hooks/useAuth';
import { useContributions } from '../../hooks/useContributions';
import { ImageUploadField } from '../forms/ImageUploadField';
import { ImageUploadRepository } from '../../repositories/images';
import type { ContributionChange, ContributionTargetType, ContributionType, SerializableValue } from '../../types/contribution';

type Field = { field: string; label: string; value: SerializableValue };
export function ContributionForm({ targetType, targetId, fields, onClose }: { targetType: ContributionTargetType; targetId: string; fields: Field[]; onClose: () => void }) {
  const { user } = useAuth(); const hook = useContributions(user);
  const [type, setType] = useState<ContributionType>('general'); const [title, setTitle] = useState(''); const [description, setDescription] = useState(''); const [summary, setSummary] = useState(''); const [references, setReferences] = useState(''); const [field, setField] = useState(fields[0]?.field ?? 'title'); const [proposed, setProposed] = useState(''); const [changes, setChanges] = useState<ContributionChange[]>([]); const [image, setImage] = useState<File | null>(null); const [busy, setBusy] = useState(false); const [feedback, setFeedback] = useState('');
  const selected = fields.find((item) => item.field === field);
  const add = () => { if (!selected || !proposed.trim()) return setFeedback('Informe a alteração proposta.'); const proposedValue = Array.isArray(selected.value) ? proposed.split(',').map((x) => x.trim()).filter(Boolean) : proposed.trim(); setChanges((current) => [...current.filter((x) => x.field !== field), { field, label: selected.label, previousValue: selected.value, proposedValue }]); setProposed(''); };
  const submit = async (event: FormEvent) => { event.preventDefault(); if (!user || !description.trim() || !summary.trim() || !changes.length) return setFeedback('Preencha descrição, resumo e ao menos uma alteração.'); setBusy(true); let images: string[] = []; if (image) { if (!ImageUploadRepository) { setBusy(false); return setFeedback('Upload indisponível.'); } const uploaded = await ImageUploadRepository.uploadImage(targetType === 'problem' ? 'problem-images' : 'solution-images', user.id, image); if (!uploaded.ok) { setBusy(false); return setFeedback(uploaded.message); } images = [uploaded.url]; } const result = await hook.createContribution({ targetType, targetId, contributionType: type, payload: { title: title.trim() || undefined, description: description.trim(), summary: summary.trim(), references: references.split('\n').map((x) => x.trim()).filter(Boolean), images, changes } }); setBusy(false); if (!result.ok) return setFeedback(result.message); setFeedback('Contribuição enviada para moderação.'); window.setTimeout(onClose, 700); };
  return <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/50 p-4" role="dialog" aria-modal="true"><form onSubmit={submit} className="max-h-[92vh] w-full max-w-3xl overflow-y-auto rounded-[2rem] bg-white p-6"><div className="flex justify-between"><div><h2 className="text-2xl font-semibold">Contribuir</h2><p className="text-sm text-muted">A publicação só muda depois da aprovação.</p></div><button type="button" onClick={onClose} aria-label="Fechar"><X /></button></div><div className="mt-5 grid gap-4"><label>Tipo<select className="ml-2 rounded-xl border p-2" value={type} onChange={(e: ChangeEvent<HTMLSelectElement>) => setType(e.target.value as ContributionType)}><option value="general">Melhoria geral</option><option value="correction">Correção</option><option value="update">Atualização</option><option value="evidence">Nova evidência</option></select></label><label className="grid gap-1">Título (quando aplicável)<input className="rounded-2xl border p-3" value={title} onChange={(e: ChangeEvent<HTMLInputElement>) => setTitle(e.target.value)} /></label><label className="grid gap-1">Descrição<textarea required className="rounded-2xl border p-3" value={description} onChange={(e: ChangeEvent<HTMLTextAreaElement>) => setDescription(e.target.value)} /></label><label className="grid gap-1">Resumo da alteração<textarea required className="rounded-2xl border p-3" value={summary} onChange={(e: ChangeEvent<HTMLTextAreaElement>) => setSummary(e.target.value)} /></label><label className="grid gap-1">Referências (uma URL por linha)<textarea className="rounded-2xl border p-3" value={references} onChange={(e: ChangeEvent<HTMLTextAreaElement>) => setReferences(e.target.value)} /></label><ImageUploadField label="Imagem de apoio" value={image} removed={false} alt="Prévia da imagem de apoio" onChange={setImage} onRemove={() => setImage(null)} /><div className="rounded-2xl border p-4"><div className="grid gap-2 md:grid-cols-2"><select className="rounded-xl border p-2" value={field} onChange={(e: ChangeEvent<HTMLSelectElement>) => setField(e.target.value)}>{fields.map((x) => <option key={x.field} value={x.field}>{x.label}</option>)}</select><textarea className="rounded-xl border p-2" placeholder="Valor proposto" value={proposed} onChange={(e: ChangeEvent<HTMLTextAreaElement>) => setProposed(e.target.value)} /></div><button type="button" onClick={add} className="mt-2 rounded-full bg-slate-950 px-4 py-2 text-white">Adicionar alteração</button>{changes.map((x) => <p key={x.field} className="mt-2 text-sm">{x.label}: {String(x.proposedValue)} <button type="button" className="text-rose-700" onClick={() => setChanges((c) => c.filter((y) => y.field !== x.field))}>remover</button></p>)}</div></div><p aria-live="polite" className="mt-3 text-sm font-semibold">{feedback || hook.error}</p><button disabled={busy} className="mt-4 rounded-full bg-teal-700 px-5 py-3 font-semibold text-white disabled:opacity-50">{busy ? 'Enviando...' : 'Enviar contribuição'}</button></form></div>;
}
