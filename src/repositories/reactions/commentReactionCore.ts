import type { CommentReactionSummary, CommentReactionType } from '../../types/discussion.ts';
export const COMMENT_REACTION_TYPES: readonly CommentReactionType[]=['like','support','interesting','needsEvidence'];
export type CommentReactionSummaries=Record<string,CommentReactionSummary>;
export type ParseResult={ok:true;data:CommentReactionSummaries}|{ok:false;message:string};
export const emptyCommentReactionSummary=():CommentReactionSummary=>({like:{count:0,selected:false},support:{count:0,selected:false},interesting:{count:0,selected:false},needsEvidence:{count:0,selected:false}});
export function parseCommentReactionSummaries(data:unknown):ParseResult{if(!Array.isArray(data))return{ok:false,message:'Não foi possível interpretar as reações.'};const summaries:CommentReactionSummaries={};for(const row of data as Record<string,unknown>[]){if(typeof row?.comment_id!=='string'||!COMMENT_REACTION_TYPES.includes(row.reaction_type as CommentReactionType)||typeof row.reaction_count!=='number'||!Number.isSafeInteger(row.reaction_count)||row.reaction_count<0||typeof row.selected_by_user!=='boolean')return{ok:false,message:'Não foi possível interpretar as reações.'};summaries[row.comment_id]??=emptyCommentReactionSummary();summaries[row.comment_id][row.reaction_type as CommentReactionType]={count:row.reaction_count,selected:row.selected_by_user}}return{ok:true,data:summaries}}
