import assert from 'node:assert/strict';import{readFileSync}from'node:fs';import test from'node:test';
import{parsePublicMemberProfile,publicProfilePage}from'../src/repositories/publicProfiles/publicProfileCore.ts';
import{pageFromHash,hashFromPage}from'../src/routing/hashRouter.ts';import{publicProfilePtBR,publicProfileEnUS}from'../src/i18n/locales/publicProfile.ts';
const migration=readFileSync('supabase/migrations/20260803130000_sprint48_public_member_profile.sql','utf8'),page=readFileSync('src/pages/PublicProfile.tsx','utf8'),comments=readFileSync('src/components/discussions/CommentCard.tsx','utf8'),workflow=readFileSync('.github/workflows/deploy.yml','utf8');
const valid={status:'public',profile:{userId:'u',username:'ana',displayName:'Ana',avatarUrl:null,bio:null,organization:null,city:null,state:null,country:null,website:'https://example.org',role:'member',joinedAt:'2026-08-03T00:00:00Z',metrics:{reputation:1,comments:1,discussions:1,reactionsReceived:1,bestAnswers:0,problems:1,solutions:0,approvedContributions:0},achievements:[{key:'active_voice',earnedAt:'2026-08-03T00:00:00Z'}],activity:[]}};
test('strict parser accepts contract and rejects malformed data',()=>{assert.equal(parsePublicMemberProfile(valid).ok,true);for(const bad of [{},null,{...valid,profile:{...valid.profile,metrics:{}}},{...valid,profile:{...valid.profile,website:'javascript:alert(1)'}},{status:'not_found',email:'x@y'}])assert.equal(parsePublicMemberProfile(bad).ok,false)});
test('username routing round trips',()=>{assert.equal(publicProfilePage(' Ana '),'member:ana');assert.equal(hashFromPage('member:ana'),'#/members/ana');assert.equal(pageFromHash('#/members/Ana'),'member:Ana')});
test('async states, cancellation and safe links are explicit',()=>{for(const token of ["kind:'loading'","kind:'error'","status==='private'",'publicProfile.notFound','AbortController','request.current','noopener noreferrer','aria-live'])assert.ok(page.includes(token),token)});
test('both locales contain every message',()=>{assert.deepEqual(Object.keys(publicProfilePtBR),Object.keys(publicProfileEnUS));for(const value of Object.values(publicProfileEnUS))assert.doesNotMatch(value,/perfil|conquista|problema publicado/i)});
test('achievements and author links are translated',()=>{assert.match(page,/reputation\.achievement\.\$\{a\.key\}/);assert.match(comments,/members\/\$\{encodeURIComponent\(comment\.authorUsername\)\}/);assert.match(comments,/publicProfile\.openAuthor/)});
test('server privacy, aggregation, grants and CI are present',()=>{assert.match(migration,/security definer set search_path=pg_catalog,public/);assert.match(migration,/if not v_profile\.public_profile/);assert.match(migration,/order by occurred_at desc,kind,id limit 20/);assert.match(migration,/grant execute[\s\S]*to anon,authenticated/);assert.doesNotMatch(migration,/auth\.users|execute format/i);assert.match(workflow,/sprint48_public_profile_assertions\.sql/);assert.match(workflow,/npm run test:sprint48/)});
